# Time Entry Components

UI components for displaying time entries.

## Components

- **TimeEntryRow.svelte** — Single time entry row showing time range, duration, client/contract context, description, and edit/delete actions. Uses inline delete confirmation pattern.
